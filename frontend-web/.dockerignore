node_modules
dist
*.log

# Local-only config; production values come from --build-arg (see Dockerfile).
.env

# Dev/integration harnesses (need a running backend; not part of the image).
verify-*.mjs
test-*.mjs

.git
.gitignore
.idea
.vscode
.DS_Store
Dockerfile
.dockerignore
README.md

# macOS AppleDouble resource-fork files (created on external volumes)
._*
